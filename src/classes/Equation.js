class Equation {
    constructor(id, fittedFunction, startTime, endTime, timeSteps) {
        this.id = id;
        this.fittedFunction = fittedFunction;
        this.startTime = startTime;
        this.endTime = endTime;
        this.timeSteps = timeSteps;
    }
}

export default Equation;